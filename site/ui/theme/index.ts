import fontSize from './FontSize'
import { border, colors, fontFamily, tabsRadius } from './Theme'
import borderRadius from './BorderRadius'
import lineHeight from './LineHeight'


export const theme = {
  fontSize,
  colors,
  fontFamily,
  borderRadius,
  lineHeight,
  tabsRadius,
  border
}